# Soprano TTS Requirements
# Install with: pip install -r requirements.txt

# Core dependency
soprano-tts
gradio==5.50

# Note: PyTorch is installed separately via torch.js with CUDA support
# Do not add torch here - it will be handled by the Pinokio installer

# Additional dependencies (installed automatically with soprano-tts)
# Listed here for reference:
# transformers>=4.30.0
# numpy>=1.24.0
# scipy>=1.10.0
# soundfile>=0.12.0
# torchaudio>=2.0.0

# Optional: LMDeploy for faster inference (may not install on all systems)
# lmdeploy>=0.2.0
